# Backend
PORT=5000

# MongoDB Atlas
# Replace with your own Atlas URI in .env
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.pq5nvwi.mongodb.net/impactly?retryWrites=true&w=majority&appName=Cluster0

# JWT
JWT_SECRET=replace_with_a_long_random_secret
